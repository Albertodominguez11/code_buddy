def ordenar_numeros(lista):
    for i in range(len(lista)):
        for j in range(len(lista) - 1):
            if lista[j] > lista[j+1]:
                lista[j], lista[j+1] = lista[j+1], lista[j]
    return lista
print("funcion que ordena numeros")

print("llamda a la funcion")

lista = [2, 7, 3, 9, 1]
ordenar_numeros(lista)
print("lista ordenada")
print("Codigo terminado")
print(".")

print("suma de dos numeros:")
def suma(a,b):
    return a+b
resultado = suma(1+2)
print("la suma de 1 y 2 es:", resultado)
